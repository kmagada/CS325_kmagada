name: Docker CI

on:
  push:
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Build Docker image for tests
        run: |
          cd project1
          docker build -t project1:test .

      - name: Build main docker image
        run: |
          cd project1
          docker build -t myapp .

      - name: Run tests
        run: |
          docker run --rm myapp pytest -v
